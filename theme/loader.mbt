///|
/// Theme layout loader — discovers layout files.

///|
/// Load layout files from the theme layouts directory.
pub fn load_layouts(_theme_path : String) -> Map[String, String] {
  // TODO: read theme/layouts/*.html from disk
  // For MVP, return empty map — build pipeline uses built-in default layout
  {}
}
