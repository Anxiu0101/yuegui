///|
/// Typst RenderAdapter: renders Typst via external CLI.
/// Stub — will shell out to `typst compile` in Phase 2.

///|
/// Render a Typst document. Stub.
pub fn render(_parsed : String) -> String {
  // TODO: invoke typst compile --features html --format html
  return ""
}
