///|
/// Markdown ParserAdapter: extracts frontmatter and parses body.
///
/// Will use core types (SourceDocument, ParsedDocument, etc.) when implemented.
/// Currently stubbed to compile.

///|
/// Parse a Markdown source document. Stub.
pub fn parse(_source : String) -> String {
  // TODO: parse frontmatter, headings, links
  // TODO: return ParsedDocument (needs import of kozmosa/yuegui/core)
  return ""
}
